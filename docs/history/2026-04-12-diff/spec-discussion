新增架構圖 diff 功能
在編輯區按下 diff 按鈕, 編輯區分成上下兩區. 
檢視區把上下兩個架構圖節點合併在同一張圖片中, 依不同顏色/highlight 方式, 標示出上下兩個架構圖 diff 的節點.
只標示節點即可, 連線資訊可以無須標示.

請問需求是否有待釐清部分?

---

  有幾個地方需要確認：

  1. Diff 比較基準：上方是「原始」、下方是「修改後」？還是兩邊沒有主從關係？
  2. Diff 標示分類：我預設這樣區分，可以嗎？
    - 只存在於上方的節點（刪除）→ 紅色
    - 只存在於下方的節點（新增）→ 綠色
    - 兩邊都有但屬性不同（修改）→ 黃色
    - 兩邊相同的節點 → 預設色（無標示）

  3. 「屬性不同」的定義：節點名稱相同但 tags、icon、所屬 zone 等有變動，算修改？還是只比較節點是否存在？
  4. 合併圖的佈局：zone 結構不同時，以哪邊的 zone 為主來排版？還是取聯集？
  5. 退出 diff：再按一次 diff 按鈕就回到單一編輯區模式

---

1. 原則上上方是原始、下方是修改後，但也可能沒有主從關係。以架構圖 diff 來說，是否有主從關係不應是問題，故一律視為無主從關係
2. 可以
3. 屬性不同比對 icon, 所屬 zone. tags 不算修改.
4. 取聯集
5. 按下 diff 之後, 按鈕改成 exit / merge 兩個按鈕.
   - exit: 回到單一編輯區模式
   - merge: 把兩個架構圖合併在一起後回到單一編輯區

請問有待釐清項目嗎? 若無, 請輸出 implementation plan / tasks 至 docs/history/2026-04-12-diff

---

  還有一個需要確認：

  merge 合併的邏輯：當兩邊有衝突時（同名節點但 icon/zone 不同），merge 以哪邊為準？還是保留下方（修改後）的版本？

---

有衝突時 merge 以下方（修改後）的版本為準

請問有待釐清項目嗎? 若無, 請輸出 implementation plan / tasks 至 docs/history/2026-04-12-diff
